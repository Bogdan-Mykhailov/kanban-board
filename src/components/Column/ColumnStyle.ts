import {CSSProperties} from "react";

export const cardWrapper: CSSProperties = {
  overflow: 'scroll',
  width: '100%',
  height: '100%',
  display: 'flex',
  flexDirection: 'column',
  alignItems: 'center',
}
